[build]
dockerfilePath = "apps/server/Dockerfile"
buildContext = "."

[deploy]
startCommand = "node dist/apps/server/src/main"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
